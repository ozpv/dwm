#!/bin/sh
case "$(echo "Poweroff \nReboot ﰇ\nSuspend ⏾\nCancel ﰸ" | dmenu)" in
    "Poweroff ") loginctl poweroff ;;
    "Reboot ﰇ") loginctl reboot ;;
    "Suspend ⏾") loginctl suspend ;;
    "Cancel ﰸ") exit ;;
    *) exit ;;
esac
